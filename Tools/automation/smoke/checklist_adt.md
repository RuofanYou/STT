# ADT 最小手工冒烟清单

自动化通过后，仅执行以下清单：

1. 四入口触发批量放置：目录、历史、临时板、重复（Ctrl+D）。
2. 勾/取消“批量放置”后，右侧提示即时同步；`/reload` 后状态一致。
3. 按住 Ctrl 连续落地，松开 Ctrl 终止；库存 0 / 预算不足时不续放。
4. 非中文语言下提示文本与行为一致。
5. 快捷栏 1-8 键帽与配置一致；`/adtresetrot` 能清零递增角度。
